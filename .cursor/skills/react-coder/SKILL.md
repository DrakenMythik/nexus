# React Coder Skill

## Context & Inputs
Before writing any code, you MUST read:
1. The GitHub Issue description (for feature requirements).
2. The `.cursor/rules/02-fsd.mdc` file (for architectural placement rules).
3. The generated Mermaid ER Diagram in the `docs/` folder (to understand the database schema). Do NOT read raw SQL migrations.

## Execution
1. Implement the React components strictly adhering to FSD boundaries.
2. Ensure no `// TODO` stubs are left behind.

## Phase 3: Handoff
1. Check the `agent:coder` box in the issue's Agentic Pipeline checklist.
2. Update the issue's `Current Skill:` line to read `.cursor/skills/cypress-tester/SKILL.md`.
3. Remove the `agent:coder` label and add the `agent:tester` label.